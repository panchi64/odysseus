"""One module per feature, each exposing a ``MANIFEST: FeatureManifest``.

Dropping a module here *is* registering the feature — discovery walks the package;
there is no list to edit. See ``harness/manifest.py`` for what a manifest declares.
"""
