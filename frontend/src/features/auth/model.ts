/** Auth feature — types.
 *
 * Single-operator, password-unlock backend: no usernames, no 2FA, no roles. The
 * screens just collect a password and call the session store (`setup`/`unlock`).
 */
export {};
