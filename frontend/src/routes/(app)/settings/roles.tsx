import { RoleBindingsSection, SettingsLayout } from "~/features/settings";

export default function RoleBindingsRoute() {
  return (
    <SettingsLayout>
      <RoleBindingsSection />
    </SettingsLayout>
  );
}
